# Local Development Source Notes

Local development should let the BFF and frontend validate API contracts without a deployment.

The demo project includes prebuilt wiki and cache artifacts so the read path works immediately.
